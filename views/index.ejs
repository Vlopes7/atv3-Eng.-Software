<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Portfólio</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">


  <style>
    @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&family=Teko:wght@500&display=swap');

html, body {
  width: 100%;
  overflow-x: hidden;
  background-color: #f4f7f6; 
  color: #333333;
  font-family: 'Roboto', sans-serif;
  scroll-behavior: smooth;
  line-height: 1.6;
}

#chuva {
  display: none;
}

.apresentacao {
  position: relative;
  height: 90vh;
  overflow: hidden;
  background-color: #FFFFFF;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: flex-start;
  padding: 0 10%;
  border-bottom: 1px solid #e0e0e0;
}

.nav {
  position: absolute;
  top: 0;
  right: 0;
  display: flex;
  justify-content: end;
  gap: 30px;
  font-family: "Teko", sans-serif;
  font-weight: 500;
  font-size: 22px;
  padding: 20px 40px;
  width: 100%;
  background-color: #FFFFFF;
}

.nav-link {
  color: #555555;
  text-decoration: none;
  transition: color 0.3s ease;
}

.nav-link:hover {
  color: #007BFF;
  text-shadow: none;
}

h4 {
  font-family: "Roboto", sans-serif;
  font-weight: 400;
  color: #007BFF;
  font-size: 24px;
  margin: 0;
}

#nome {
  color: #222222;
  font-size: 72px;
  font-family: "Teko", sans-serif;
  font-weight: 500;
  font-style: normal;
  margin: 0;
  text-shadow: none;
}

h3 {
  font-family: "Roboto", sans-serif;
  font-weight: 300;
  color: #555555;
  font-size: 28px;
  margin: 10px 0 0 0;
}

.titulo {
  text-align: center;
  margin-top: 80px;
  margin-bottom: 40px;
  color: #333;
  font-family: "Teko", sans-serif;
  font-weight: 500;
  font-size: 42px;
}

.sobre-container {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 40px;
  color: #333;
  font-family: "Roboto", sans-serif;
  font-weight: 300;
  font-size: 18px;
  margin: 80px auto 0 auto; 
  width: 100%;
  max-width: 900px;
}

.foto-perfil {
  width: 200px;
  height: 200px;
  border-radius: 50%;
  object-fit: cover;
  object-position: center 35%;
  border: 4px solid #007BFF;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.sobre-texto {
  max-width: 600px;
}

.container {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  justify-content: center;
  align-items: stretch;
  padding: 0 20px;
}

.card {
  width: 280px;
  min-height: 150px; 
  background-color: #FFFFFF;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.05);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center; 
  text-align: center;
  padding: 20px;
  transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
  font-family: "Roboto", sans-serif;
  color: #444;
  font-size: 18px;
}

.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 6px 12px rgba(0,0,0,0.1);
}

.card p {
  margin: 5px 0;
}

.card strong {
  color: #007BFF;
  font-weight: 700;
}

.projeto-container {
  display: flex;
  flex-wrap: wrap;
  gap: 30px;
  justify-content: center;
  align-items: stretch;
  padding: 0 20px;
}

.projeto-card {
  width: 320px;
  min-height: 300px;
  background-color: #FFFFFF;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.05);
  padding: 25px;
  transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
  font-family: "Roboto", sans-serif;
  color: #444;
  font-size: 16px;
  line-height: 1.5;
  display: flex;
  flex-direction: column;
}

.projeto-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 6px 12px rgba(0,0,0,0.1);
}

.projeto-card strong {
  font-size: 20px;
  font-family: 'Teko', sans-serif;
  color: #007BFF;
  margin-bottom: 10px;
}

.projeto-card p {
  flex-grow: 1;
  margin-top: 10px;
}

.tec-label {
  margin-top: 15px;
  color: #333;
  font-size: 16px;
  font-weight: 700;
  margin-bottom: 5px;
}

.tec-nomes {
  color: #555;
  font-size: 14px;
  font-style: italic;
  margin: 0;  
}

.contato-container {
  display: flex;
  gap: 25px;
  justify-content: center;
  align-items: center;
  margin-top: 100px;
  margin-bottom: 80px;
  padding: 40px 0;
  background-color: #FFFFFF;
  border-top: 1px solid #e0e0e0;
}

.contato-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 50px;
  height: 50px;
  border: 2px solid #e0e0e0; 
  border-radius: 50%; 
  color: #555; /* Cor do ícone */
  font-size: 22px;
  transition: all 0.3s ease;
}

.contato-link:hover {
  transform: scale(1.1);
  background-color: #007BFF; /* Fundo azul ao passar o mouse */
  color: #FFFFFF; /* Ícone branco */
  border-color: #007BFF;
  box-shadow: 0 4px 10px rgba(0, 123, 255, 0.3);
}
  </style>
</head>
<body>

  <header>
    <section class="apresentacao">
      <canvas id="chuva"></canvas>
      <nav class="nav">
        <a class="nav-link" href="#sobre">Sobre</a>
        <a class="nav-link" href="#formacao">Formação</a>
        <a class="nav-link" href="#projetos">Projetos</a>
        <a class="nav-link" href="#contato">Contato</a>
      </nav>
      <h4>Olá, eu sou</h4>
      <p id="nome"><%= dadosPessoais.nome %></p>
      <h3><%= dadosPessoais.descricao %></h3>
    </section>
  </header>
  <main>
      <section id="sobre">
        <div class="sobre-container">
        <img src="/imagem.jpg" alt="foto" class="foto-perfil">
        <div class="sobre-texto">
          <p><%= sobre.apresentacao %></p>
        </div>
      </div>
      </section>
      <section>
        <h2 class="titulo">Soft Skills</h2>
        <div class="container">
          <% softSkills.forEach(skill => { %>
            <div class="card">
              <p><%= skill.habilidade %></p>
            </div>
          <% }) %>
        </div>
      </section>
        <section>
      <h2 class="titulo">Hard Skills</h2>
        <div class="container">
          <% hardSkills.forEach(hard => { %>
            <div class="card">
              <p><strong><%= hard.nomeHabilidade %></p></strong><br>
              <p><%= hard.habilidade %></p>
            </div>
          <% }) %>
        </div>
      </section>
     <section id="formacao">
      <h2 class="titulo">Cursos e Certificações</h2>
      <div class="container">
        <% formacoes.forEach(f => { %>
          <div class="card">
            <strong><%= f.curso %></strong><br>
            <%= f.instituicao %><br>
            (<%= f.ano %>)
          </div>
        <% }) %>
      </div>
    </section>
     <section id="projetos">
        <h2 class="titulo">Projetos</h2>
        <div class="projeto-container">
          <% projetos.forEach(p => { %>
        <div class="projeto-card">
          <strong><%= p.titulo %></strong><br>
          <p><%= p.descricao %></p>
          <% if (p.tecnologias) { %>
            <p class="tec-label"><strong>Tecnologias:</strong></p>
            <p class="tec-nomes"><%= p.tecnologias %></p>
              <% } %>
          </div>
        <% }) %>
      </div>
    </section>
    <section id="contato">
      <div class="contato-container">
        <% if (contato.email) { %>
          <a href="mailto:<%= contato.email %>" class="contato-link" target="_blank">
            <i class="fa-solid fa-envelope"></i>
          </a>
        <% } %>
       <% if (contato.github) { %>
      <a href="<%= contato.github %>" class="contato-link" target="_blank">
        <i class="fa-brands fa-github"></i>
      </a>
    <% } %>
    <% if (contato.linkedin) { %>
      <a href="<%= contato.linkedin %>" class="contato-link" target="_blank">
        <i class="fa-brands fa-linkedin"></i>
      </a>
    <% } %>
  </div>
</section>

  </main> 
  <script src="styles.js"></script>
</body>
</html>